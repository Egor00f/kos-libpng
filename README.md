# libpng for kolibrios

This is libpng from kolibrios repository.

## Build

### Depends

+ newlib
+ zlib

### Build targets

static library:
```
make liblibpng16.a
```

shared library:
```
make libpng16.dll
```

package:
```
make kos-libpng-dev
```
